name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows-portable:
    name: Build Windows portable exe
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run checks
        run: npm run check

      - name: Build Windows portable package
        run: npm run package:win
        env:
          # The portable build is intentionally unsigned. Disable electron-builder's
          # code-signing certificate auto-discovery so it doesn't probe signtool /
          # download winCodeSign on the runner (a common CI failure point).
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/AgentDeck-*.exe
          generate_release_notes: true
